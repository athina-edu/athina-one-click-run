version: '2'
services:
  athina-web:
    image: athinaedu/athina-web:latest
    command: gunicorn --bind :8000 athinaweb.wsgi:application
    volumes:
      - ./athina_files/:/code/athina_files/
      - ./db.sqlite3:/code/db.sqlite3
      - ./athinaweb/settings_secret.py:/code/athinaweb/settings_secret.py
    expose:
      - "8000"
  nginx:
    image: nginx
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./nginx/error.log:/etc/nginx/error_log.log
      - ./nginx/cache/:/etc/nginx/cache
      - ./certs/:/etc/certs/
    ports:
      - 80:80
      - 443:443
      - 8000:8000
    depends_on:
      - "athina-web"
  athina:
    image: athinaedu/athina:latest
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./athina_files/:/code/athina_files/
      - ./logs/:/code/logs/
      - /tmp:/tmp
    depends_on:
      - "nginx"
    command: ["--json", "http://127.0.0.1:80/assignments/api", "-s", "-v"]
    network_mode: "host"
